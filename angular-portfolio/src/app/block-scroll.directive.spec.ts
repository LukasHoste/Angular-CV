import { BlockScrollDirective } from './block-scroll.directive';

describe('BlockScrollDirective', () => {
  it('should create an instance', () => {
    const directive = new BlockScrollDirective();
    expect(directive).toBeTruthy();
  });
});
